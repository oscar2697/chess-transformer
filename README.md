# Chess-Transformer
Research project: searchless Transformer for chess (policy/value) with attention interpretability.

## Quick Start
```bash
pip install -r requirements.txt
python -m src.graph_agents.graph  # runs DAG stub
```

## Structure
See CONTEXT.md and SKILLS.md
